/**
 *  a dictionary of testID necessary for testing.
 *
 * TO use it:
 * import it as: `import testIDs from '@@config/testIDs'`
 * then attach it to whatever JSX element you need to reference from the test suite.
 *
 * ```
 * <Button title="what ever" onPress={() => {}} testID={testIDs.WHATEVER_BUTTON}/>
 * ```
 */

export default {
  TOP_BAR_FIND_PLACE: 'TOP_BAR_FIND_PLACE',
};
